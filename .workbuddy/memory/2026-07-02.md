## Phase 8-11 完成 + 全量复习

### Phase 8: 角色权限（AOP）
- User 实体新增 role 字段，数据库 user 表加 role 列
- 自定义注解 @RequiresPermission + PermissionAspect 切面
- 修复关键 bug: JwtUtil.generateToken() 中 .claim("role", "user") 硬编码，改成 .claim("role", role)
- 只有 admin 角色能删除用户，普通 user 返回 403

### Phase 9: 日志系统
- 新增 logback-spring.xml 配置（日志写文件，按天切割，保留30天）
- 升级 LogAspect.java（记录 HTTP方法、URL、用户名、IP、耗时、异常）
- 升级 GlobalExceptionHandler.java（用 logger 替代 printStackTrace，业务异常 WARN，系统异常 ERROR）
- 移除 WebConfig 中的 LogInterceptor 注册（与 LogAspect 功能重复）
- 删除 TestAspect.java（含 System.out.println("around")）

### Phase 10: 定时任务
- 启动类加 @EnableScheduling
- 新建 task/ScheduledTask.java
- 三种模式: fixedRate(10s心跳)、fixedDelay(5s统计用户数)、cron(每分钟清理)

### Phase 11: 全局限流
- 新建 annotation/RateLimit.java（window + max 两个参数）
- 新建 aspect/RateLimitAspect.java（滑动窗口算法，ConcurrentHashMap + Deque）
- UserController.getUserById 加 @RateLimit(window=60, max=5)
- 测试通过：第6次请求返回 429

### 全量复习
- 生成了 SpringBoot复习手册.md（完整复习文档）
- 绘制了 3 个架构图：整体架构、完整请求流程、技能树总览

### 项目当前状态
- 32 个 Java 文件
- 11 个 Phase 全部完成
- 用户偏好：教学模式，不帮改文件（除非用户说"你做"），要指明文件和行号

### 详细复习手册（增强版）
- 用户要求"再详细点"，生成了 SpringBoot详细复习手册_完整版.md
- 读取全部 31 个源码文件，提取真实代码和行号
- 新增内容：pom.xml 依赖详解、每个文件完整源码+逐行注释、AOP动态代理原理、JWT结构详解、滑动窗口算法图解、fixedRate vs fixedDelay 时间轴图、SecurityContext 原理、BCrypt 原理、Postman 测试步骤、12 道面试题、完整文件结构树
