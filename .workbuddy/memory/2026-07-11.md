# 2026-07-11

## TaskService 单元测试完成
- 文件：`src/test/java/com/example/springbootpractice/service/TaskServiceTest.java`
- 5个测试方法全部通过（exit code 0）：
  1. `getTaskById_Success` — 查到任务，验证 title
  2. `getTaskById_NotFound` — 查不到抛 BusinessException
  3. `updateTaskStatus_Success` — 0→1 正常流转
  4. `updateTaskStatus_InvalidTransition` — 0→2 跳级抛异常
  5. `updateTaskStatus_TaskNotFound` — 任务不存在抛异常
- 使用 `@ExtendWith(MockitoExtension.class)` + `@Mock` + `@InjectMocks` 模式
- OpenJDK warning 是无害的 VM 警告，不影响测试结果
