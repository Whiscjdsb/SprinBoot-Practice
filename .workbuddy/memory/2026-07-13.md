# 2026-07-13

## RabbitMQ 集成 — 全部完成并验证通过 ✅

### 代码清单
- RabbitConfig.java: TopicExchange + Queue + Binding（task.notification.queue / task.notification.exchange / task.create）
- TaskNotificationProducer.java: 注入 RabbitTemplate，sendTaskCreatedMessage() 发送消息
- TaskNotificationConsumer.java: @RabbitListener 监听队列，模拟发送通知
- docker-compose.yml: 添加 rabbitmq:3.13-management-alpine 服务（5672/15672），配置 healthcheck + depends_on
- application.properties: spring.rabbitmq 连接配置
- pom.xml: spring-boot-starter-amqp 依赖
- TaskService.java: 用户自己写了 Producer 注入 + createTask() 中调用 sendTaskCreatedMessage()

### 验证结果
Docker 构建成功，4 容器 healthy。日志确认完整链路通过：
[生产者] 消息已发送 -> [消费者] 收到消息 -> 模拟发送通知 -> 处理完成

### 踩坑
- init.sql 密码是明文 123456，但 AuthController 用 BCrypt 校验 → 登录 401
- 修复：Python bcrypt 生成哈希 → 更新数据库 + init.sql
- Docker 镜像源 docker.1ms.run 拉不到 maven → 加 dockerpull.org 备选源
- PowerShell 没有 grep → 用 Select-String 替代

## Nacos 服务注册中心 — 第1步完成 ✅

### 代码变更
- pom.xml: 加 spring-cloud.version=2023.0.1 + spring-cloud-alibaba.version=2023.0.1.0 + nacos-discovery 依赖 + dependencyManagement BOM
- application.properties: spring.cloud.nacos.discovery.server-addr=${NACOS_HOST:localhost}:8848
- docker-compose.yml: app 加 NACOS_HOST:host.docker.internal + extra_hosts 强制 IPv4

### Nacos 本地运行（不用 Docker）
- 下载 nacos-server-2.3.2.zip → C:\Users\Whiscjdsb\Documents\nacos\
- 修改 startup.cmd 第58行加 -Dserver.port=8848（JAR 内置配置覆盖外部配置问题）
- 启动方式：JAVA_HOME=D:\develop\jdk + startup.cmd -m standalone

### 验证结果
Nacos 8848 正常运行，Spring Boot 注册成功：
[REGISTER-SERVICE] springboot-practice 172.18.0.6:8080 register finished

### 踩坑
- Nacos Docker 镜像拉不下来（TLS timeout）→ 改本地跑
- Nacos 启动端口跑偏 50582 而非 8848 → JAR 内置配置覆盖 conf/application.properties → startup.cmd 加 -Dserver.port=8848
- Docker 容器连 host Nacos IPv6 不可达 → docker-compose 加 extra_hosts: host.docker.internal:host-gateway
- nacos/nacos-server:v2.3.2 国内镜像都拉不动 → 手工下载解压本地跑
